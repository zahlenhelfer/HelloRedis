version: "3"
services:
  javaclient:
    build: .
    networks:
      - helloredis-net
    depends_on:
      - redis
  redis:
    image: redis
    container_name: redisdb
    networks:
      - helloredis-net
      
networks:
  helloredis-net:
